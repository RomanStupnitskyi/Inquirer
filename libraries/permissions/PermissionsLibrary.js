import { BaseLibrary } from "../../core/structures/BaseLibrary.js";

export class PermissionsLibrary extends BaseLibrary {
	constructor(inquirer, properties = {}) {
		super(inquirer, properties);
	}
}
